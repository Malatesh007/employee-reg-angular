import { UpdatecolorDirective } from './updatecolor.directive';

describe('UpdatecolorDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdatecolorDirective();
    expect(directive).toBeTruthy();
  });
});
